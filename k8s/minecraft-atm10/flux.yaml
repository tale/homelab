apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: minecraft-atm10
  namespace: flux-system
spec:
  dependsOn:
    - name: infra-configs
  interval: 1h
  path: ./k8s/minecraft-atm10
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
