apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: external-dns
  namespace: flux-system
spec:
  dependsOn:
    - name: cert-manager
  interval: 1h
  path: ./k8s/infra/external-dns
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
