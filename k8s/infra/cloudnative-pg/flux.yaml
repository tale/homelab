apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cloudnative-pg
  namespace: flux-system
spec:
  dependsOn:
    - name: openebs
  interval: 1h
  path: ./k8s/infra/cloudnative-pg
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
