apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-operator
  namespace: flux-system
spec:
  interval: 1h
  chart:
    spec:
      chart: flux-operator
      version: ">=0.40.0"
      sourceRef:
        kind: HelmRepository
        name: flux-operator
  values:
    web:
      enabled: true
      httpRoute:
        enabled: true
        parentRefs:
          - name: envoy-gw
            namespace: default
            sectionName: https
        hostnames:
          - flux.tale.me
