apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - manifests/namespace.yaml

  # vanilla — survival world migrated from lab-cloud. Tailscale-only access
  # (mc.heron-mahi.ts.net); no LAN LoadBalancer.
  - manifests/vanilla/pvc.yaml
  - manifests/vanilla/deployment.yaml
  - manifests/vanilla/service-tailscale.yaml
