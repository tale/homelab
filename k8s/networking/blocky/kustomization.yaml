apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - blocky-db-uri.sops.yaml
  - blocky-db-credentials.sops.yaml
  - grafana-datasource.yaml
  - service-monitor.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: blocky-config
    namespace: networking
    files:
      - config/config.yml

configurations:
  - kustomizeconfig.yaml
