apiVersion: v1
kind: Secret
metadata:
  name: home-assistant-restic
  namespace: home-automation
stringData:
  RESTIC_REPOSITORY: s3:https://${B2_ENDPOINT}/${B2_BUCKET}/volsync/home-assistant
  RESTIC_PASSWORD: ${RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${B2_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${B2_APP_KEY}
---
apiVersion: v1
kind: Secret
metadata:
  name: eufy-security-ws-restic
  namespace: home-automation
stringData:
  RESTIC_REPOSITORY: s3:https://${B2_ENDPOINT}/${B2_BUCKET}/volsync/eufy-security-ws
  RESTIC_PASSWORD: ${RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${B2_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${B2_APP_KEY}
---
apiVersion: v1
kind: Secret
metadata:
  name: scrypted-restic
  namespace: home-automation
stringData:
  RESTIC_REPOSITORY: s3:https://${B2_ENDPOINT}/${B2_BUCKET}/volsync/scrypted
  RESTIC_PASSWORD: ${RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${B2_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${B2_APP_KEY}
