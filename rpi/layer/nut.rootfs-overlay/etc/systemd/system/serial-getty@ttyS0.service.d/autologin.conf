# Serial console recovery over the Pi's UART (115200 8N1, enable_uart=1 is
# already set by the base image). Same reasoning as the tty1 drop-in.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin core --keep-baud 115200,57600,38400,9600 %I $TERM
