# The Pi is on the UPS but must not self-shutdown — that would drop upsd for the
# Talos secondaries. Monitor only; it rides the battery and dies last on its own.
MINSUPPLIES 1
MONITOR cyberpower@localhost 1 upsmon cluster-local secondary
SHUTDOWNCMD "/bin/true"
